Installation
============

The easiest way to add Ungrasp to your Python code is using `uv`:

.. code-block:: sh

    uv add ungrasp

Advanced Setup
--------------

You can also use pip:

.. code-block:: sh

    pip install ungrasp
